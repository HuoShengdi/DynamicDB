import React, { PropTypes } from 'react';

const SearchField = React.createClass({
  render () {
    return (
      <div>
        <form>

        </form>
      </div>
    );
  }
});

export default SearchField;
